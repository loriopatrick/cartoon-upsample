use quadtree::{QuadTree, Region};

struct Point {
    x: u32,
    y: u32,
}

//struct Shape {
//    leafs: Vec<ShapeComponent>,
//}
//
//fn build_shape(tree: &mut QuadTree,
//               shapes: &mut Vec<Shape>, regions: &mut Vec<Region>) {
//}
